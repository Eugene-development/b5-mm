# Маскирование секретного ключа в формах

## Обзор
Добавлено маскирование секретного ключа в формах для повышения безопасности. Теперь ключ отображается как `**********************ABCD` (только последние 4 символа видны).

## Реализация

### Проект b5-mm (публичная форма)
**Файл:** `b5-mm/src/routes/+page.svelte`

**Логика:**
- При вводе ключа он отображается полностью (фокус на поле)
- При потере фокуса ключ маскируется автоматически
- Показываются только последние 4 символа
- Остальные символы заменяются звездочками `*`

**Поведение:**
```
Ввод:     01HZY8Y9G5F8M9B6W7K3NQ4Z8X
Фокус:    01HZY8Y9G5F8M9B6W7K3NQ4Z8X (полный ключ)
Без фокуса: **********************4Z8X (маскированный)
```

**Подсказка:** "Ключ скрыт для безопасности. Кликните для редактирования."

### Проект b5-agent (модальное окно)
**Файл:** `b5-agent/src/lib/components/ProjectSubmitModal.svelte`

**Логика:**
- Ключ подставляется автоматически из профиля
- По умолчанию отображается маскированным
- Кнопка "глаз" для переключения видимости
- Поле readonly (нельзя редактировать)

**Поведение:**
```
По умолчанию: **********************4Z8X (маскированный)
После клика:  01HZY8Y9G5F8M9B6W7K3NQ4Z8X (полный ключ)
```

**Иконки:**
- 👁️ Открытый глаз - показать ключ
- 👁️‍🗨️ Перечеркнутый глаз - скрыть ключ

## Функция маскирования

```javascript
function maskSecretKey(key) {
  if (!key || key.length <= 4) {
    return key || '';
  }
  const visiblePart = key.slice(-4);
  const maskedPart = '*'.repeat(key.length - 4);
  return maskedPart + visiblePart;
}
```

**Примеры:**
- `01HZY8Y9G5F8M9B6W7K3NQ4Z8X` → `**********************4Z8X`
- `ABC` → `ABC` (короткие ключи не маскируются)
- `""` → `""` (пустая строка)

## Преимущества

1. **Безопасность** - ключ не виден посторонним при вводе
2. **Удобство** - можно показать полный ключ при необходимости
3. **Консистентность** - одинаковое поведение во всех формах
4. **UX** - понятные подсказки и иконки

## Различия между формами

| Характеристика | b5-mm (публичная) | b5-agent (модальная) |
|----------------|-------------------|----------------------|
| Редактирование | ✓ Да | ✗ Нет (readonly) |
| Автозаполнение | ✗ Нет | ✓ Да |
| Показ/скрытие | По фокусу | По кнопке |
| Иконка | ✗ Нет | ✓ Да (глаз) |

## Состояния

### b5-mm
```javascript
let secretKeyInput = $state('');        // Реальное значение
let isKeyFocused = $state(false);       // Фокус на поле
let displayValue = $derived(            // Отображаемое значение
  isKeyFocused ? secretKeyInput : maskSecretKey(secretKeyInput)
);
```

### b5-agent
```javascript
let secretKeyInput = $state('');        // Реальное значение
let showFullKey = $state(false);        // Показать полный ключ
let displayValue = $derived(            // Отображаемое значение
  showFullKey ? secretKeyInput : maskSecretKey(secretKeyInput)
);
```

## Тестирование

### b5-mm
1. Откройте форму на сайте
2. Начните вводить секретный ключ
3. Проверьте, что ключ виден полностью при вводе
4. Кликните вне поля
5. Убедитесь, что ключ замаскирован (видны только последние 4 символа)
6. Кликните снова на поле - ключ должен стать видимым

### b5-agent
1. Откройте модальное окно создания проекта
2. Проверьте, что ключ замаскирован по умолчанию
3. Нажмите на иконку глаза
4. Убедитесь, что ключ стал полностью видимым
5. Нажмите снова - ключ должен замаскироваться

## Безопасность

✓ Ключ не виден при просмотре через плечо  
✓ Скриншоты не покажут полный ключ (если не раскрыт)  
✓ Реальное значение всегда хранится в памяти  
✓ Маскирование только визуальное (не влияет на отправку)

## Связанные файлы
- `b5-mm/src/routes/+page.svelte` - публичная форма
- `b5-agent/src/lib/components/ProjectSubmitModal.svelte` - модальное окно
- `b5-agent/src/routes/(protected)/profile/+page.svelte` - профиль (оригинальная реализация)
